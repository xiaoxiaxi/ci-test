language: node_js
node_js:
    - "8.10.0"
install:
    - npm install
# keep the npm cache around to speed up installs
cache:
    directories:
        - "$HOME/.npm"
script:
    - npm run lint
    - npm run test